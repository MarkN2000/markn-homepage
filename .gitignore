# Hugo
/public/
/resources/
.hugo_build.lock

# Node.js / npm
/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# package-lock.json は通常バージョン管理に含めますが、
# もし含めたくない場合は以下のコメントを外してください。
# package-lock.json
# yarn.lock (Yarn を使用している場合)

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor directories and files
# VSCode
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
# 特定のVSCode設定を共有したい場合は、上記のように!で始まる行で除外設定を解除できます。
# プロジェクトで共有すべきでないユーザー設定は無視するのが一般的です。
# .idea/ (IntelliJ系)
# *.swp (Vim)

# Netlify (もし Netlify を使う場合)
# .netlify

# Environment variables
# .env

# Sass cache
.sass-cache/

# Cache folder for PostCSS (if any, typically handled by node_modules or resources)
# .cache/

# Log files
logs
*.log

# Sub-repositories
static/koerec/.git

# User specified
static/koerec/